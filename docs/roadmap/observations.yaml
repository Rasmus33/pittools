# Worker-Knowledge — committetes Wissens-Substrat (ADR #78, erweitert ADR #77).
#
# EINE committete Datei (NICHT gitignored) für alles, was OTTO während einer
# Iteration an Wissen erzeugt. Überlebt Worktree-/Klon-Wechsel + Multi-User;
# Git-Log ist die Per-Iteration-History (ADR #15) — kein Archiv-Ordner.
#
# Drei Bereiche mit GETRENNTER Reset-Semantik (ADR #78):
#
#   entries       Mid-Loop-Beobachtungen der LAUFENDEN Iteration (`observation
#                 add`): surprises[] + PO-Reasoning. Wird bei `iteration-done`
#                 geleert (vorher vom Audit nach learnings.md destilliert).
#                 Schema je Eintrag:
#                   - at: <ISO-8601-UTC>      # Zeitstempel
#                     issue: <nr|null>        # auslösendes Ticket (optional)
#                     po_class: <A..H|Q|null> # PO-Klasse falls zuordenbar
#                     surprises: [<str>, ...]  # Beobachtungen ohne Action-Bedarf
#                     reasoning: <str>         # Klartext / Wurzelursachen-Hunch
#
#   discoveries   Mid-Loop-Kandidaten der LAUFENDEN Iteration (`observation
#                 discovery-add`): Pattern-/Antipattern-/Shared-Item-Kandidaten
#                 + Cross-Feature-Smells. Vorher in gitignored .state.yaml →
#                 jetzt committet (ADR #78). Reife (≥3 Evidenz ∧ ≥2 Tickets)
#                 wird hieraus rekonstruiert. Wird bei `iteration-done` geleert.
#                 Schema je Kandidat: {slug, evidence: [<file:line>], issues:
#                 [<nr>], class?, note?}.
#
#   seeds_next    VORWÄRTS-Seeds für die NÄCHSTE Iteration (`observation seed`,
#                 vom Audit am Iter-Ende geschrieben). Wird beim Start der
#                 Folge-Iteration von Vision/Gap konsumiert (`observation
#                 consume-seeds`) — NICHT bei iteration-done geleert. Ersetzt
#                 das deprecatete gitignored `iteration-seeds.yaml` (ADR #78).
schema_version: 1.4.0
iteration: 0
entries:
- at: '2026-08-14T22:32:20Z'
  issue: 1
  po_class: B
  surprises:
  - 'guard-test.js ist zeilenenden-empfindlich: LF-Checkout gruen, CRLF-Worktree (autocrlf=true)
    rot — Java-Kommentarzeilen mit Anfuehrungszeichen rutschen in die Literal-Extraktion.
    Betrifft JEDES Worktree-Final-Gate auf Windows.'
  - 'Implementer #1 hat den Fund korrekt als out-of-scope behandelt und per Baseline-Verifikation
    (git archive) belegt statt zu raten.'
  reasoning: 'Wurzelursache ist die Extraktion (nicht die Java-Kommentare) — Fix als
    P0-Task #12 eingeschoben (Variant b), da alle Final-Gates in frischen Worktrees
    betroffen sind. #1 wartet mit Review bis der Fix gemergt und der Branch nachgezogen
    ist.'
- at: '2026-08-14T22:41:49Z'
  issue: 12
  po_class: null
  surprises:
  - "Wurzelursache des guard-test-CRLF-Bruchs: JS-Regex $ matcht nicht vor \r und\
    \ . matcht \r nicht — der Kommentar-Strip war auf CRLF-Zeilen ein stilles No-op.\
    \ Fix: CRLF-Normalisierung in literalsFromJavaBlock() + synthetischer Regressionstest,\
    \ checkout-unabhaengig."
  reasoning: 'Task #12 sauber durch: nur app/guard-test.js, Validator PASS, alle Gates
    gruen im CRLF-Worktree. Damit sind die Final-Gates fuer alle folgenden Tickets
    wieder verlaesslich.'
- at: '2026-08-14T22:46:58Z'
  issue: 1
  po_class: null
  surprises:
  - 'SI #1 sauber: reportError-Helfer additiv neben diagError, 183/183 Tests, keine
    Call-Site angefasst — Konsumenten-Umstellung folgt in den Feature-Lifts bzw. der
    Folge-Iteration.'
  reasoning: Shared-Item zuerst gemergt, damit die 4 Konsumenten-Features den Helfer
    nutzen koennen. Version 4.37.0 geht mit dem Merge live auf beide Handys — rein
    additive Aenderung, kein Verhaltensrisiko.
- at: '2026-08-14T23:03:58Z'
  issue: 2
  po_class: null
  surprises:
  - 'Feature-Lift #2: alle 6 stillen Netz-/Cache-Pfade der App loggen jetzt via reportNetError
    in den Ringpuffer; Zustands-Setter statt Direktmutation; 2 neue statische guard-test-Checks.
    Java ohne Compiler — Validator hat den Diff zeilenweise auf Syntax geprueft.'
  - 'APK-Build steht aus: versionCode 11 / versionName 1.7.0 gemergt, aber Installation
    kann nur Rasmus (Keystore).'
  reasoning: Groesste RA-Luecke (48/56) strukturell geschlossen; Wirkung misst das
    Audit. Build-Schritt als Handoff an Rasmus vermerkt.
- at: '2026-08-14T23:24:36Z'
  issue: 3
  po_class: D
  surprises:
  - 'Validator PARTIAL bei #3: Akzeptanz voll erfuellt, Findings sind Folgearbeit
    (Test-Garantie enger ziehen, lastEligible sichtbar machen) — als Discoveries geroutet
    statt Re-Spawn, da kein aktueller Defekt und alles additiv.'
  reasoning: 'PO-Entscheidung: Merge trotz PARTIAL — alle 190 Tests gruen, Report
    nur additiv veraendert, Findings betreffen Zukunfts-Garantien, nicht Ist-Verhalten.
    Version 4.38.0 geht live.'
- at: '2026-08-14T23:44:27Z'
  issue: 4
  po_class: D
  surprises:
  - 'Slots-Fix #4 macht das Batch-Sicherheitsnetz erstmals scharf — dabei neu entstandene
    Verhaltensflaeche: formationSlots wird bei jedem Challenge-Wechsel auf Default
    11 zurueckgesetzt, Brick-Slot-SBCs koennten im Retry-Fenster transient mismatchen.
    Fail-safe (Abbruch statt Falscheintrag), Korrektur idR synchron.'
  - matchedAs musste an die wert-gegateten Bedingungen gekoppelt werden statt an die
    Scope-Flags (PLAYER_LEVEL-Dual-Use) — exakt der Bug, den das Feld sichtbar machen
    soll.
  reasoning: 'PARTIAL akzeptiert und gemergt: alle Akzeptanzkriterien erfuellt, Finding
    ist eine ungetestete (nicht falsche) Timing-Flaeche mit sauberem Fail-Verhalten.
    Diagnose-Protokollierung des formationSlots-Verlaufs wird in Ticket #5 (batch-modus)
    mit umgesetzt — dort gehoert sie hin.'
- at: '2026-08-15T00:01:42Z'
  issue: 5
  po_class: D
  surprises:
  - 'reportError hatte bis #5 projektweit KEINEN Aufrufer — onBatchPlanClick ist die
    erste echte Adoption; die uebrigen Konsumenten-Stellen kommen mit #8/#9/#10.'
  - Beleg-Koordinaten verschieben sich zwischen Phasen-Commits desselben Tickets (22
    Zeilen) — Validator hat die ADR-87-Registrierung auf :4910 korrigiert.
  reasoning: 'PARTIAL akzeptiert und gemergt: Kontrollfluss byte-identisch additiv,
    Abbruch-Disziplin unveraendert, 224/224. v4.40.0 geht live.'
discoveries:
  pattern_candidates:
  - slug: symmetrie-test-lesen-schreiben-trennen
    evidence:
    - solver-test.js:1465-1480
    issues:
    - 3
    class: good
    note: Der STATE.diag-Symmetrie-Test sollte Schreiben (=, .push(, .shift(, ++)
      von Lesen unterscheiden statt nur Referenz-ausserhalb zu pruefen — sonst besteht
      ein nur-gelesenes Feld (uiScan-Klasse). 1 Beleg, noch unreif.
  antipattern_candidates: []
  shared_item_candidates: []
  cross_feature_smells:
  - slug: reportneterror-304-nicht-fehler
    evidence:
    - app/java/com/sbctools/browser/MainActivity.java:464-467
    issues:
    - 2
    note: severity=P3; 304-Zweig (Cache aktuell) laeuft durch den Fehler-Chokepoint
      reportNetError — funktional harmlos, aber begriffliche Unschaerfe; Kandidat
      fuer Audit-Sammelticket.
  - slug: lasteligible-nie-im-report
    evidence:
    - ea-fc-sbc-optimizer.user.js:122
    - ea-fc-sbc-optimizer.user.js:2685
    issues:
    - 3
    note: severity=P3; STATE.diag.lastEligible wird bei 403 echt zugewiesen, aber
      nie in buildDiagReport ausgegeben — Spiegelbild des uiScan-Falls (befuellt-aber-nie-sichtbar).
      Vorbestehend, Kandidat Audit-Sammelticket.
  - slug: roadmap-md-q7-drift-batch-navigation
    evidence:
    - docs/ROADMAP.md:38-58
    issues:
    - 5
    note: severity=P3; ROADMAP.md Punkt 1b beschreibt Batch-Navigation als ungeloest
      (Stand v4.18.1), obwohl clickSetTile/clickChallengeRow laengst funktionieren
      — Q7-Drift, Kandidat Audit-Sammelticket.
  - slug: code-geography-nicht-resolver-kompatibel
    evidence:
    - docs/roadmap/vision/features/batch-modus.md
    - docs/roadmap/vision/features/rating-solver.md
    issues:
    - 5
    note: severity=P3; code_geography der Userscript-Features nutzt beschreibende
      Eintraege (Datei — Funktionsliste), die der Beleg-/Scorer-Resolver nicht als
      Datei aufloest (candidate_files=0) — pattern add-beleg fuer diese Features unmoeglich.
      Vision-Docs beim Audit auf reine Pfade/Globs umstellen.
seeds_next:
  seeded_at: null
  patterns: []
  shared_items: []
  features: []
  discoveries: []
