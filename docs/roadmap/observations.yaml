# Worker-Knowledge — committetes Wissens-Substrat (ADR #78, erweitert ADR #77).
#
# EINE committete Datei (NICHT gitignored) für alles, was OTTO während einer
# Iteration an Wissen erzeugt. Überlebt Worktree-/Klon-Wechsel + Multi-User;
# Git-Log ist die Per-Iteration-History (ADR #15) — kein Archiv-Ordner.
#
# Drei Bereiche mit GETRENNTER Reset-Semantik (ADR #78):
#
#   entries       Mid-Loop-Beobachtungen der LAUFENDEN Iteration (`observation
#                 add`): surprises[] + PO-Reasoning. Wird bei `iteration-done`
#                 geleert (vorher vom Audit nach learnings.md destilliert).
#                 Schema je Eintrag:
#                   - at: <ISO-8601-UTC>      # Zeitstempel
#                     issue: <nr|null>        # auslösendes Ticket (optional)
#                     po_class: <A..H|Q|null> # PO-Klasse falls zuordenbar
#                     surprises: [<str>, ...]  # Beobachtungen ohne Action-Bedarf
#                     reasoning: <str>         # Klartext / Wurzelursachen-Hunch
#
#   discoveries   Mid-Loop-Kandidaten der LAUFENDEN Iteration (`observation
#                 discovery-add`): Pattern-/Antipattern-/Shared-Item-Kandidaten
#                 + Cross-Feature-Smells. Vorher in gitignored .state.yaml →
#                 jetzt committet (ADR #78). Reife (≥3 Evidenz ∧ ≥2 Tickets)
#                 wird hieraus rekonstruiert. Wird bei `iteration-done` geleert.
#                 Schema je Kandidat: {slug, evidence: [<file:line>], issues:
#                 [<nr>], class?, note?}.
#
#   seeds_next    VORWÄRTS-Seeds für die NÄCHSTE Iteration (`observation seed`,
#                 vom Audit am Iter-Ende geschrieben). Wird beim Start der
#                 Folge-Iteration von Vision/Gap konsumiert (`observation
#                 consume-seeds`) — NICHT bei iteration-done geleert. Ersetzt
#                 das deprecatete gitignored `iteration-seeds.yaml` (ADR #78).
schema_version: 1.4.0
iteration: 9
entries:
- at: '2026-08-15T21:58:01Z'
  issue: 66
  po_class: null
  surprises:
  - 'Die Max-teure-Entfernung legte eine tote generische DP-Dimension frei (exp/expDims
    an allen Call-Sites null) — dokumentiert statt entfernt (DP-Kern-Verbot); Muster:
    Feature-Entfernungen hinterlassen tote Generik, die erklaert werden muss.'
  - Validator verifizierte die ungetestete Filter-Interaktion selbst per Probe-Skript,
    bevor er sie als Finding meldete — Findings mit eigenem Beweis sind sofort umsetzbar.
  reasoning: 'Panel-Rework auf User-Wunsch: Max-teure (Soft-Limit, fuehlte sich kaputt
    an) ersatzlos raus als dokumentierte Ausnahme; harter Max-Rating-Filter am solve()-Eingang
    (SSOT fuer Einzel+Batch); Erweiterte Einstellungen in 4 Gruppen. v4.62.0, 582/582.'
- at: '2026-08-15T22:15:04Z'
  issue: 70
  po_class: null
  surprises:
  - Der Implementer fand selbststaendig eine Stale-Diagnose-Falle (staleRecover einer
    FRUEHEREN Aufloesung koennte als 0-Kandidaten fehlgelesen werden) und baute einen
    setId-Frische-Guard ein — Diagnose-Felder brauchen Frische-Semantik, wenn Entscheidungen
    daran haengen.
  reasoning: Erschoepfung/Ablauf nach Submit-404 heisst jetzt beim Namen (Keine weitere
    Wiederholung verfuegbar — N von M geschafft), der Neu-oeffnen-Rat bleibt nur fuer
    den uneindeutigen Fall; staleRecover traegt Server-Zustand (status/timesCompleted/endTime).
    v4.63.0, 598/598, Main-verifiziert.
discoveries:
  pattern_candidates: []
  antipattern_candidates: []
  shared_item_candidates: []
  cross_feature_smells: []
seeds_next:
  seeded_at: '2026-08-15T20:36:39Z'
  patterns: []
  shared_items: []
  features:
  - 'PORTFOLIO KOMPLETT (iter8): alle 9 Features exakt auf structural_max — solver
    95/95, pool 85/85, diagnose 85/85, bedienpanel 85/85, sbc 80/80, android 80/80,
    ea-app 75/75, team 75/75, batch 70/70. Weitere Punkte gibt es nur noch ueber Cap-Revisionen
    (neue Erkenntnisse/EA-Wandel), nicht ueber Lifts.'
  discoveries:
  - 'Offen ausserhalb der Scores: Joint-Optimierungs-Debt (§41 Bekannte Grenze, anlassgebunden),
    APK-1.8.0-Installation, Live-Bewaehrung v4.60/v4.61 durch Rasmus'
