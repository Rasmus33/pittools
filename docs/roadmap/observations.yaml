# Worker-Knowledge — committetes Wissens-Substrat (ADR #78, erweitert ADR #77).
#
# EINE committete Datei (NICHT gitignored) für alles, was OTTO während einer
# Iteration an Wissen erzeugt. Überlebt Worktree-/Klon-Wechsel + Multi-User;
# Git-Log ist die Per-Iteration-History (ADR #15) — kein Archiv-Ordner.
#
# Drei Bereiche mit GETRENNTER Reset-Semantik (ADR #78):
#
#   entries       Mid-Loop-Beobachtungen der LAUFENDEN Iteration (`observation
#                 add`): surprises[] + PO-Reasoning. Wird bei `iteration-done`
#                 geleert (vorher vom Audit nach learnings.md destilliert).
#                 Schema je Eintrag:
#                   - at: <ISO-8601-UTC>      # Zeitstempel
#                     issue: <nr|null>        # auslösendes Ticket (optional)
#                     po_class: <A..H|Q|null> # PO-Klasse falls zuordenbar
#                     surprises: [<str>, ...]  # Beobachtungen ohne Action-Bedarf
#                     reasoning: <str>         # Klartext / Wurzelursachen-Hunch
#
#   discoveries   Mid-Loop-Kandidaten der LAUFENDEN Iteration (`observation
#                 discovery-add`): Pattern-/Antipattern-/Shared-Item-Kandidaten
#                 + Cross-Feature-Smells. Vorher in gitignored .state.yaml →
#                 jetzt committet (ADR #78). Reife (≥3 Evidenz ∧ ≥2 Tickets)
#                 wird hieraus rekonstruiert. Wird bei `iteration-done` geleert.
#                 Schema je Kandidat: {slug, evidence: [<file:line>], issues:
#                 [<nr>], class?, note?}.
#
#   seeds_next    VORWÄRTS-Seeds für die NÄCHSTE Iteration (`observation seed`,
#                 vom Audit am Iter-Ende geschrieben). Wird beim Start der
#                 Folge-Iteration von Vision/Gap konsumiert (`observation
#                 consume-seeds`) — NICHT bei iteration-done geleert. Ersetzt
#                 das deprecatete gitignored `iteration-seeds.yaml` (ADR #78).
schema_version: 1.4.0
iteration: 3
entries:
- at: '2026-08-15T13:08:06Z'
  issue: 42
  po_class: null
  surprises:
  - Symmetrie-Test deckte scanStats automatisch ohne neue Ausnahmen ab — das direkte
    =-Schreibmuster zahlt sich aus (Design aus iter1 traegt).
  - 441/441 nach +32 Tests; Baseline 409 im Diff nachweislich nur Insertions (Q2-Beleg
    per git diff statt Behauptung).
  reasoning: 'Glattes PASS. EA-Wandel-Linse Teil 1: unbekannte Requirement-Scopes,
    gekappte Scans und reqCount-Defaults sind jetzt im Report sichtbar, BEVOR etwas
    falsch gebaut wird. v4.49.0.'
- at: '2026-08-15T13:25:18Z'
  issue: 43
  po_class: null
  surprises:
  - Vermeintliche Phasen-Abweichung (kein separater release-Commit) war plankonform
    — die Lift-Plaene zweier paralleler Tickets definierten das Mapping unterschiedlich;
    Validator hat gegen den JEWEILS EIGENEN Plan geprueft statt pauschal.
  - 'Das reportError-Gating brauchte keinen neuen Code: der bestehende !kind-Early-Return
    erledigt es strukturell — am Code verifiziert statt geglaubt.'
  reasoning: 'Glattes PASS. EA-Wandel-Linse Teil 2: JSON-Parse-Fehler an SBC-URLs
    landen in lastErrors, unbekannte /ut/game/-Endpunkte werden gezaehlt+gesampelt,
    neue rareflag-Werte sind trotz Top-5-Cap sichtbar. v4.50.0.'
- at: '2026-08-15T13:38:27Z'
  issue: 44
  po_class: null
  surprises:
  - Der Waechter-Extraktions-Umbau lief mit Byte-Gleichheits-Beweis beider Wege an
    der ECHTEN MainActivity — dieselbe Neutralitaets-Technik wie beim extractFunction-Umbau
    in iter1 (#31), bewaehrt sich als Standard fuer Testwerkzeug-Refactorings.
  - MainActivity-Diff exakt +2/-0 Kommentarzeilen — der Beweis, dass Test-Infra-Haertung
    ohne jedes Runtime-Risiko moeglich ist (kein versionCode-Bump, kein APK noetig).
  reasoning: 'Glattes PASS. Test-Infra-Runde: javac-Gate ohne Keystore (compile-check.sh)
    faengt Java-Syntaxfehler kuenftig VOR dem PO-Build; Waechter-Extraktion marker-basiert
    mit Literal-Fallback statt nur inzidenteller Literale. Kein Produkt-Verhalten
    geaendert.'
discoveries:
  pattern_candidates: []
  antipattern_candidates: []
  shared_item_candidates: []
  cross_feature_smells: []
seeds_next:
  seeded_at: '2026-08-15T13:42:53Z'
  patterns: []
  shared_items: []
  features:
  - 'Roadmap ist konvergiert: alle 9 Features an/1 Punkt unter ihren strukturellen
    Deckeln (android 80/80, ea-app 75/75, team 75/75, sbc 79/80, batch 69/70, diagnose
    84/85, spieler-pool 83/85, bedienpanel 82/85, solver 92/95). Weitere Lifts nur
    bei neuem Anlass (EA-Umbau, Live-Befund, Cap-Revision durch neue Erkenntnisse).'
  - 'sbc-vorgaben-erkennung: applyFromSetChallenges ruft deepScanChallenge(node) ohne
    recordDeepScanStats — scanStats.deepScan zeigt im haeufigen Fallback-Pfad (Response
    ohne target) die Metriken des frueheren Scans (Restfund aus dem iter3-Re-Score,
    klein, additiv fixbar)'
  discoveries:
  - 'ea-app-anbindung: apiBaseDetectionStuck + sbcHookMisses bewusst verschoben (duenn,
    0 Punkte Luft am Deckel)'
  - 'APK-Installation durch Rasmus weiter ausstehend: v1.8.0 liegt in app/build/ —
    die Userscript-Updates v4.49/4.50 kommen dagegen automatisch auf beide Handys'
  - 'compile-check.sh ist da, aber noch NICHT als final_gate in config/system.yaml
    registriert — Kandidat: als vierter commands-Eintrag aufnehmen, dann laeuft es
    bei jedem kuenftigen Ticket automatisch'
