# Worker-Knowledge — committetes Wissens-Substrat (ADR #78, erweitert ADR #77).
#
# EINE committete Datei (NICHT gitignored) für alles, was OTTO während einer
# Iteration an Wissen erzeugt. Überlebt Worktree-/Klon-Wechsel + Multi-User;
# Git-Log ist die Per-Iteration-History (ADR #15) — kein Archiv-Ordner.
#
# Drei Bereiche mit GETRENNTER Reset-Semantik (ADR #78):
#
#   entries       Mid-Loop-Beobachtungen der LAUFENDEN Iteration (`observation
#                 add`): surprises[] + PO-Reasoning. Wird bei `iteration-done`
#                 geleert (vorher vom Audit nach learnings.md destilliert).
#                 Schema je Eintrag:
#                   - at: <ISO-8601-UTC>      # Zeitstempel
#                     issue: <nr|null>        # auslösendes Ticket (optional)
#                     po_class: <A..H|Q|null> # PO-Klasse falls zuordenbar
#                     surprises: [<str>, ...]  # Beobachtungen ohne Action-Bedarf
#                     reasoning: <str>         # Klartext / Wurzelursachen-Hunch
#
#   discoveries   Mid-Loop-Kandidaten der LAUFENDEN Iteration (`observation
#                 discovery-add`): Pattern-/Antipattern-/Shared-Item-Kandidaten
#                 + Cross-Feature-Smells. Vorher in gitignored .state.yaml →
#                 jetzt committet (ADR #78). Reife (≥3 Evidenz ∧ ≥2 Tickets)
#                 wird hieraus rekonstruiert. Wird bei `iteration-done` geleert.
#                 Schema je Kandidat: {slug, evidence: [<file:line>], issues:
#                 [<nr>], class?, note?}.
#
#   seeds_next    VORWÄRTS-Seeds für die NÄCHSTE Iteration (`observation seed`,
#                 vom Audit am Iter-Ende geschrieben). Wird beim Start der
#                 Folge-Iteration von Vision/Gap konsumiert (`observation
#                 consume-seeds`) — NICHT bei iteration-done geleert. Ersetzt
#                 das deprecatete gitignored `iteration-seeds.yaml` (ADR #78).
schema_version: 1.4.0
iteration: 6
entries:
- at: '2026-08-15T16:12:47Z'
  issue: 56
  po_class: null
  surprises:
  - Der wichtigste iter6-Fund (loadIncomplete in keinem Diagnose-Kanal + Batch-Plan
    prueft nicht) sass ausgerechnet im Feature mit der zweitbesten Wertung — Verifikations-Runden
    lohnen sich genau dort, wo man fertig zu sein glaubt.
  reasoning: Unvollstaendiger Pool ist jetzt in Report (clubLoad.loadIncomplete),
    App-Log (warn) und Batch-Vorschau (Banner + plan.poolLoadIncomplete) sichtbar
    — Planen bleibt moeglich, Rasmus entscheidet bei der Freigabe. removeFromPool
    + Lock-Einzelkey-Fehler haengen am reportError-Chokepoint. v4.56.0, 526/526, Takt
    zeilenidentisch.
- at: '2026-08-15T16:40:32Z'
  issue: 57
  po_class: null
  surprises:
  - 'Das erweiterte Fuzzing fand beim ERSTEN Lauf einen echten Solver-Defekt: die
    Rarity-Reservierung waehlt rein nach costOf und kann das maxOvershoot-Fenster
    sprengen, obwohl eine fensterkonforme Loesung existiert (Minimal-Repro: 91er-Storage-Special
    statt 84er -> waste 3.06 bei Fenster 0). Dreifach gegen unabhaengige Enumerationen
    verifiziert — die Referenz stimmt, der Solver nicht.'
  - 'Charakterisierungstest-Muster etabliert: BEKANNTER-BEFUND-Check pinnt das IST-Verhalten
    (main-Gate bleibt gruen) und schlaegt laut an, sobald der Fix kommt.'
  reasoning: 'Test-only: Eligible-Gate (Formel-Drift-Schutz) erstmals ausgefuehrt,
    Fuzzing auf Rarity-/Qualitaets-Quoten + planBatch ausgeweitet (535 Tests). Der
    gefundene Reservierungs-Defekt wird als eigenes Fix-Ticket umgesetzt — Regel-Hierarchie
    laut CLAUDE.md: Fenster > Kosten.'
- at: '2026-08-15T18:01:52Z'
  issue: 60
  po_class: null
  surprises:
  - 'Der Fuzzing-Befund fuehrte zum tiefsten Solver-Eingriff seit iter0: runSearch
    musste zu searchTeam parametrisiert werden (Closure->Parameter, Mathe wortgleich
    — Validator hat jede Zeile verglichen). Zwei Folge-Teilfehler (globalVmin aus
    rohem vMin; vMinFloor auch final) fand der Implementer selbst per ~6000-Fall-Fuzzing,
    BEVOR er den Fix als korrekt meldete.'
  - Mid-Ticket-Rebase auf zwei Live-Hotfixes funktionierte sauber via SendMessage
    an denselben Implementer (Kontext intakt) — Konflikte nur an den erwarteten Stellen.
  reasoning: 'Solver respektiert jetzt die dokumentierte Regel-Hierarchie Fenster
    > Kosten auch bei Rarity-Vorgaben: fensterbewusste Reservierung (Trial-Cap 200,
    DP-bewertet) mit dem alten Greedy als textuell identischem Fallback. v4.59.0,
    548/548, Validator-PASS ohne Findings.'
discoveries:
  pattern_candidates: []
  antipattern_candidates: []
  shared_item_candidates: []
  cross_feature_smells: []
seeds_next:
  seeded_at: '2026-08-15T18:38:40Z'
  patterns: []
  shared_items: []
  features:
  - 'rating-solver: playerLevelConstraints-Reservierung hat strukturell denselben
    Kosten-zuerst-Aufbau wie der gefixte Rarity-Pfad (user.js ~2293-2307) — offener,
    UNVERIFIZIERTER Verdacht aus LEARNINGS §41; naechster Schritt waere ein gezieltes
    Fuzzing wie in #57, NICHT blind fixen'
  - EA hat am 15.08. abends grosse Kit-/Belohnungs-Metadaten in die Set-Daten gehaengt
    — Ausloeser der Hotfix-Kaskade v4.57-v4.60; die elgReq-Quelle (v4.60) ist jetzt
    die Verteidigungslinie, Live-Bewaehrung durch Rasmus stand beim Seed noch aus
  discoveries:
  - 'Live-Feedback-Schleife funktionierte: Report -> Diagnose-Feld zeigt Ursache ->
    Hotfix in <1h, viermal am selben Abend; die iter3-6-Diagnosefelder (scanStats,
    deepScanBySource, titleSource, skippedKeys) waren dabei die Werkzeuge'
  - LEARNINGS §30 traegt noch eine changelog-artige Formulierung (pre-existierend,
    vom iter6-Re-Score notiert) — Mini-Doku-Politur bei Gelegenheit
  - APK v1.8.0-Installation durch Rasmus weiterhin ausstehend
